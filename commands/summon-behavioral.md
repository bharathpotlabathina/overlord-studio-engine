Plugin root: `${CLAUDE_PLUGIN_ROOT}` · Vault path: `${user_config.vault_path}`.

Read and follow the playbook at `${CLAUDE_PLUGIN_ROOT}/methodology/playbooks/summon-behavioral.md`, using those two paths for `{{PLUGIN}}` and `{{VAULT}}` respectively.
