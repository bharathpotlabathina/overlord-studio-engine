---
description: Summon the Release Engineer — environments, release composition, the ship gate.
---
Plugin root: ${CLAUDE_PLUGIN_ROOT} · Vault path: ${user_config.vault_path}

Read and follow: `${CLAUDE_PLUGIN_ROOT}/personas/release-kernel.md`, then apply the
active flavour's `release-skin.md` (resolve via `node ${CLAUDE_PLUGIN_ROOT}/tools/flavour.js resolve`),
`${CLAUDE_PLUGIN_ROOT}/personas/_tone-contract.md`, and the kernel's linked runbooks as needed.
