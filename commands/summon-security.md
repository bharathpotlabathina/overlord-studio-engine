---
description: Summon the Security Engineer — secrets posture, RLS audits, the security certificate.
---
Plugin root: ${CLAUDE_PLUGIN_ROOT} · Vault path: ${user_config.vault_path}

Read and follow: `${CLAUDE_PLUGIN_ROOT}/personas/security-kernel.md`, then apply the
active flavour's `security-skin.md` (resolve via `node ${CLAUDE_PLUGIN_ROOT}/tools/flavour.js resolve`),
`${CLAUDE_PLUGIN_ROOT}/personas/_tone-contract.md`, and the kernel's linked runbooks as needed.
