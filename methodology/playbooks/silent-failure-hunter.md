> `{{PLUGIN}}` = the plugin root and `{{VAULT}}` = the vault path, both stated by the command that pointed you here.

# /silent-failure-hunter — Dispatch Silent Failure Hunter QA Agent

Dispatch the Silent Failure Hunter agent on the current project.

Read the agent brief at `{{PLUGIN}}/methodology/agents/silent-failure-hunter.md`, then adopt that agent's role and scan all source files in the current project directory for the patterns it defines. Report findings exactly as the brief specifies.
